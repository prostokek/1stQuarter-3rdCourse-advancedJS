module.exports = {
    entry: './index.js', // точка входа. название файла, с которого начинается сборка проекта. корневой файл (как правило)
    output: { //куда собирать
        filename: './build.js'
    },
    target: 'node', // указываем, для чего собираем
}